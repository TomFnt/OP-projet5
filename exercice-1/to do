- Déplacer le projet dans laragon

- PDO  :
    --> fonction connect X
    --> fonction create table X
    --> insert datasample X
    --> Créer les requêtes SQL X


-Classe ContactManager :
    --> Créer la fonction toString() qui permet de convertir l'objet en string pour pouvoir l'afficher X
    --> retourner l'affichage dans la méthode toString() X

-Checkpoint étape 6 (créer repo et push code dessus)

- Amélio :

--> command help X
--> command modify X
--> command delete X
--> remplacer la méthode toString() par la méthode magique __toString()

--> patch les if (format id invalide)
--> retour à la ligne dans le catch, gestion couleur dans le cmd X

--> Faire une gestion des erreurs dans le contactManager X
    -create X
    -detail X
    -modify X
    -delete X
